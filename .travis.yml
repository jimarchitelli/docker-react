sudo: required
services: 
  - docker

before_install:
  - docker build -t jmarchitelli/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true jmarchitelli/docker-react npm run test --coverage